# CineStream Backend - Example Environment Variables
# Copy to ".env" and adjust values for your environment.

# Application
APP_NAME="CineStream Backend"
APP_VERSION="1.0.0"
ENV="development"

# CORS: Comma-separated list of allowed origins.
# Important: Do not use "*" when allow_credentials=True (default in app) or browsers will block credentialed requests.
# Include your frontend origin(s) here.
# For Kavia preview environment:
CORS_ALLOW_ORIGINS="https://vscode-internal-14938-beta.beta01.cloud.kavia.ai:4000"
# For local development (uncomment if needed):
# CORS_ALLOW_ORIGINS="http://localhost:4000,http://127.0.0.1:4000"

# Auth / JWT
# Generate a strong random secret in real deployments
JWT_SECRET="change-me"
JWT_ALGORITHM="HS256"
ACCESS_TOKEN_EXPIRE_MINUTES=60

# Database - choose one of the following approaches:

# 1) Direct DATABASE_URL (recommended for production)
# Example for PostgreSQL:
# DATABASE_URL="postgresql+psycopg2://user:password@host:5432/dbname"

# 2) Separate DB components (if not using DATABASE_URL)
# DB_HOST=""
# DB_PORT=5432
# DB_NAME=""
# DB_USER=""
# DB_PASSWORD=""

# Payments (optional real gateway keys; payments are simulated by default)
# STRIPE_API_KEY=""
# PAYPAL_CLIENT_ID=""
# PAYPAL_CLIENT_SECRET=""
# UPI_PROVIDER="mock"
