# FastAPI Backend .env example for CineStream

# ----- App -----
APP_NAME=CineStream Backend
APP_VERSION=1.0.0
ENV=development

# ----- Auth / JWT -----
# Change this to a strong secret in production
JWT_SECRET=please-change-me
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60

# ----- Database -----
# Option A: Direct URL (e.g., PostgreSQL)
# DATABASE_URL=postgresql+psycopg2://user:password@localhost:5432/cinestream
# Option B: Components (used to assemble the DATABASE_URL)
# DB_HOST=localhost
# DB_PORT=5432
# DB_NAME=cinestream
# DB_USER=postgres
# DB_PASSWORD=postgres

# Development fallback: If no DB info is set, backend uses sqlite:///./dev.db

# ----- CORS -----
# IMPORTANT: Set this to your frontend origin to avoid CORS issues.
# Examples:
# CORS_ALLOW_ORIGINS=http://localhost:3000
# CORS_ALLOW_ORIGINS=https://your-frontend.example.com
CORS_ALLOW_ORIGINS=http://localhost:3000

# ----- Payments (optional, simulated by default) -----
# STRIPE_API_KEY=sk_live_xxx
# PAYPAL_CLIENT_ID=xxx
# PAYPAL_CLIENT_SECRET=xxx
# UPI_PROVIDER=mock
