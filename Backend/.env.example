# CineStream Backend - Environment Variables Example

# Application
APP_NAME=CineStream Backend
APP_VERSION=1.0.0
ENV=development
CORS_ALLOW_ORIGINS=http://localhost:3000,https://your-frontend-domain.com

# Security / Auth
JWT_SECRET=replace-with-a-long-random-secret
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60

# Database (PostgreSQL preferred)
# Option A: Single DATABASE_URL
# Example format: postgresql+psycopg2://user:password@host:5432/dbname
DATABASE_URL=postgresql+psycopg2://postgres:postgres@localhost:5432/cinestream

# Option B: Individual DB params (will be used if DATABASE_URL is empty)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=cinestream
DB_USER=postgres
DB_PASSWORD=postgres

# Payments (used for real gateway configuration if you enable them)
STRIPE_API_KEY=
PAYPAL_CLIENT_ID=
PAYPAL_CLIENT_SECRET=
UPI_PROVIDER=mock
